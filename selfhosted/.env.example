## Docker image
# Pin to a version (recommended) for reproducible deployments.
MARKDOWNBRAIN_IMAGE=ghcr.io/blackstorm/markdownbrain:latest

## Data path (optional)
# Base directory inside the container where MarkdownBrain stores DB, secrets, and local storage.
# Default in the official image is `/app/data`.
# If you change the volume mount, set DATA_PATH accordingly (for example `/data`).
DATA_PATH=/app/data

## JVM options (optional)
# Extra JVM args for the MarkdownBrain container.
# Example: JAVA_OPTS=-Xms256m -Xmx512m
JAVA_OPTS=

## Logging (optional)
# Set app log level. The official image defaults to INFO.
MARKDOWNBRAIN_LOG_LEVEL=INFO

## Caddy on-demand TLS (optional)
# Set to true to allow automatic certificate issuance for domains that exist in Console.
CADDY_ON_DEMAND_TLS_ENABLED=false

## S3/RustFS compose (only used by docker-compose.s3.yml)
# Browsers must be able to reach this URL to load published assets (images/PDF/audio/video).
S3_PUBLIC_URL=https://s3.your-domain.com

# Defaults below match the bundled RustFS container; change these if you use your own S3.
S3_ACCESS_KEY=rustfsadmin
S3_SECRET_KEY=rustfsadmin
S3_BUCKET=markdownbrain
S3_PUBLIC_PORT=9000
