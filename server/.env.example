# MarkdownBrain configuration
#
# Development:
# - Copy this file to `server/.env`.
# - Start the dev server with `make dev`.
#
# Production:
# - Prefer using Docker/Compose (see `selfhosted/`).

# -----------------------------------------------------------------------------
# Server
# -----------------------------------------------------------------------------

# HOST=0.0.0.0
# FRONTEND_PORT=8080
# CONSOLE_PORT=9090

# -----------------------------------------------------------------------------
# Database
# -----------------------------------------------------------------------------

# DB_PATH=data/markdownbrain.db

# -----------------------------------------------------------------------------
# Sessions
# -----------------------------------------------------------------------------
#
# If omitted, MarkdownBrain generates a secret and stores it in `data/.secrets.edn`
# next to the DB file.

# SESSION_SECRET=change-me

# -----------------------------------------------------------------------------
# Storage
# -----------------------------------------------------------------------------
#
# Supported: local, s3

# STORAGE_TYPE=local
# LOCAL_STORAGE_PATH=./data/storage

# -----------------------------------------------------------------------------
# S3 (required when STORAGE_TYPE=s3)
# -----------------------------------------------------------------------------

# S3_ENDPOINT=http://localhost:9000
# S3_ACCESS_KEY=your-access-key
# S3_SECRET_KEY=your-secret-key
# S3_REGION=us-east-1
# S3_BUCKET=markdownbrain
#
# Public base URL for browsers to fetch assets (images/PDF/audio/video).
# S3_PUBLIC_URL=https://s3.your-domain.com

# -----------------------------------------------------------------------------
# Environment
# -----------------------------------------------------------------------------

# ENVIRONMENT=development

# -----------------------------------------------------------------------------
# Caddy (optional)
# -----------------------------------------------------------------------------

# CADDY_ON_DEMAND_TLS_ENABLED=false
