# MarkdownBrain configuration
#
# Development:
# - Copy this file to `server/.env`.
# - Start the dev server with `make dev`.
#
# Production:
# - Prefer using Docker/Compose (see `selfhosted/`).

# -----------------------------------------------------------------------------
# Server
# -----------------------------------------------------------------------------

# HOST=0.0.0.0
# FRONTEND_PORT=8080
# CONSOLE_PORT=9090

# -----------------------------------------------------------------------------
# Data path
# -----------------------------------------------------------------------------

# Base directory for persisted data (DB, secrets, local storage).
#
# - DB: ${DATA_PATH}/markdownbrain.db
# - Secrets: ${DATA_PATH}/.secrets.edn
# - Local storage (default): ${DATA_PATH}/storage
#
# In Docker, this is usually `/app/data` (mounted as a volume). In development,
# keep it in the repo by setting it to `./data`.

# DATA_PATH=./data

# -----------------------------------------------------------------------------
# Sessions
# -----------------------------------------------------------------------------
#
# If omitted, MarkdownBrain generates a secret and stores it in `${DATA_PATH}/.secrets.edn`.

# SESSION_SECRET=change-me

# -----------------------------------------------------------------------------
# Storage
# -----------------------------------------------------------------------------
#
# Supported: local, s3

# STORAGE_TYPE=local
# LOCAL_STORAGE_PATH=./data/storage

# -----------------------------------------------------------------------------
# S3 (required when STORAGE_TYPE=s3)
# -----------------------------------------------------------------------------

# S3_ENDPOINT=http://localhost:9000
# S3_ACCESS_KEY=your-access-key
# S3_SECRET_KEY=your-secret-key
# S3_REGION=us-east-1
# S3_BUCKET=markdownbrain
#
# Public base URL for browsers to fetch assets (images/PDF/audio/video).
# S3_PUBLIC_URL=https://s3.your-domain.com

# -----------------------------------------------------------------------------
# Environment
# -----------------------------------------------------------------------------

# ENVIRONMENT=development

# -----------------------------------------------------------------------------
# Caddy (optional)
# -----------------------------------------------------------------------------

# CADDY_ON_DEMAND_TLS_ENABLED=false

# -----------------------------------------------------------------------------
# Logging (optional)
# -----------------------------------------------------------------------------
#
# Controls MarkdownBrain Logback logger level (namespace: markdownbrain).
# Default is DEBUG for development.

# MARKDOWNBRAIN_LOG_LEVEL=DEBUG
